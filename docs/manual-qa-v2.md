# 桌面灵伴 2.2 验收记录

验收环境：Windows 11 x64，当前用户安装，Qt/PySide6 6.11.1。

## 自动门禁

- `pip check`：通过，无损坏依赖。
- 源码测试：`162 passed`。
- 冻结版自检测试：`1 passed`。
- 覆盖率：`90%`，高于 85% 门槛。
- PyInstaller 冻结：通过；`DesktopCompanion.exe --self-test` 返回 `ok=true`、WebP 可用、每只宠物 74 帧。
- Inno Setup 6.7.3：成功生成“桌面灵伴安装程序.exe”。
- 真实发布验证：普通安装、自检、自启动值生命周期、卸载清理、覆盖升级、设置保留和状态恢复全部通过。

自检中的宠物清单为：

```json
{"pets":["shiyi","ziling"]}
```

## 原生界面检查

| 项目 | 结果 |
|---|---|
| 首次/默认角色 | 显示十一，窗口为 192×208，透明边缘正常 |
| 右键菜单 | 动态显示“切换宠物”子菜单，并提供“重新扫描宠物”“打开宠物目录” |
| 宠物专属动作名称 | 十一菜单使用“抬爪招呼、开心扑跳、撒娇翻肚”等名称；切换紫灵后改为“挥手问候、翩然旋舞、舒展衣袖”等人物化名称 |
| 单击回应 | 安装版十一和紫灵均在左键单击且未拖动后进入 JSON 权重选择的原地动作 |
| 双击互斥 | 安装版双击播放跳跃动作，双击判定结束后没有附带触发单击随机动作 |
| 自主小动作 | 关闭闲逛、开启注视和自主小动作后，安装版紫灵在鼠标连续静止后自行进入原地动作并恢复；自动测试覆盖 8 秒门槛、15–35 秒无注视间隔、开关和不连续重复 |
| 动作展示 | 自动测试确认依次播放六个非位移动作并在结束后恢复待机；拖动或其他动作可中断队列 |
| 十一切换到紫灵 | 单击后无需重启，窗口中立即显示紫灵 |
| 第三方宠物包 | 在 `%APPDATA%\DesktopCompanion\pets\qa_pet` 放入标准 `pet.json` 和 `spritesheet.webp` 后，安装版成功扫描并显示该图集；测试后已移除 |
| 运行时刷新 | 自动化测试覆盖新增、切换和移除当前宠物；当前宠物移除后安全回退到内置宠物 |
| 设置持久化 | 2.2 写入 `schema_version = 3`，新增 `autonomous_actions_enabled`；读取 2.1 的 schema 2 设置时保留原值并为新开关采用开启默认值 |
| 重启恢复 | 退出并重新启动后仍显示紫灵 |
| 资源契约 | 所有宠物包统一使用 v2、1536×2288、透明 WebP；注册器校验 ID、清单、大小、路径、动作占用表以及完整动作名称/自动权重；无 `actions` 的 2.1 宠物包兼容回退 |
| 静态产品图标 | 安装程序、EXE、开始菜单和桌面快捷方式使用从十一休息帧确定性生成的多尺寸 ICO |
| 动态托盘图标 | 使用当前宠物的 `iconFrame`；十一与紫灵切换时图标和“桌面灵伴 · 角色名”提示同步变化 |
| 桌面快捷方式 | 覆盖安装后实际生成 `%USERPROFILE%\Desktop\桌面灵伴.lnk`，目标为已安装的 `DesktopCompanion.exe` |
| 1.0 共存 | 2.2 延续 2.x 的独立 EXE、目录、设置、日志、自启动项、互斥量、IPC 名称和卸载 GUID，不覆盖 1.0 |

## 产品身份

- 显示名称：桌面灵伴
- 版本：2.2.0
- 可执行文件：`DesktopCompanion.exe`
- 安装目录：`%LOCALAPPDATA%\Programs\DesktopCompanion`
- 设置：`%APPDATA%\DesktopCompanion\settings.ini`
- 日志：`%LOCALAPPDATA%\DesktopCompanion\logs\DesktopCompanion.log`
- 自启动值：`HKCU\Software\Microsoft\Windows\CurrentVersion\Run\DesktopCompanion`

## 说明

原 1.0 的历史验收记录保存在 `docs/manual-qa-1.0.md`。本次没有删除或覆盖原项目、原安装包或原安装目录。2.2 覆盖升级 2.0/2.1 时保留设置和用户宠物目录；真实安装验收后已恢复测试前的十一、闲逛、注视、位置等用户设置并启动正常版。Windows 11 可能把新通知图标放入右下角 `^` 的隐藏区域，应用不强制修改用户的任务栏固定偏好。安装包未做商业 Authenticode 签名，首次从其他电脑运行时可能出现 Windows SmartScreen 提示。
