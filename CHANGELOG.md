# 更新日志

这里记录桌面灵伴每个正式版本面向用户的主要变化。新用户通常只需要安装最新版本；覆盖升级会保留现有设置和放在用户宠物目录中的自制宠物包。

## 2.4.5（2026-07-22）

### 南宫婉屋檐动作重做

- “月下含栗”由 14 帧扩展为 28 帧，补全施法伸手、紫色灵光包裹的栗子从画面左侧飞来、逐渐减速、指间悬停、接住、送入口中和收势全过程；单次播放约 4.32 秒。
- 屋檐驻留的六组动作不再只锁一个骨盆锚点，而是从腰线以下共用同一张坐姿底层；臀部、裙摆、双腿、双脚和屋檐接触处在动作间保持逐像素一致。
- 重新检查静坐、望月、小憩、夜风和侧眸的往返路径，保留头部、抬手、眼神、袖摆和发丝变化，同时禁止动作素材重绘下半身造成换座位感。

### 验证改进

- 屋檐状态总帧数由 87 增至 101，图集扩展为 3072×6240；旧动作区和 2.4.1 旧版图集备份均保持不变。
- 新增下半身逐像素锁定和栗子飞行轨迹测试：所有常驻帧的画布第 136 行以下必须与基准坐姿完全一致，栗子必须从左侧远处移动并减速停到手边。

## 2.4.4（2026-07-22）

### 修复

- 南宫婉的六组屋檐驻留动作改为锁定同一“腰胯—屋檐接触点”，同时固定横坐标和纵坐标；转头、抬手、闭目、吃栗子与衣袖发丝仍能活动，但身体不会一帧坐左边、下一帧坐右边。
- 删除坐姿呼吸和停顿阶段对整个人物做的 1–2 像素平移；呼吸感只由姿态素材本身表现，不再挪动屁股位置。
- 落座动画进入常驻状态后的最后四帧保持同一坐姿边界，避免刚坐稳又轻微弹跳。

### 验证改进

- 图集构建新增每张坐姿源图的骨盆代理锚点，并要求缩放、取整和排版后的二维偏差都不超过 0.51 像素。
- 新增骨盆锁定回归测试：即使头部位置、透明留白和源图裁切不同，坐姿根节点仍必须落在同一个目标像素附近。

## 2.4.3（2026-07-21）

### 修复

- 修复南宫婉坐在屋檐上时，因不同动作素材留白不一致而造成整个人物持续左右跳动的问题。所有坐姿帧现在以缩放后的头部中心对齐，实测最大横向漂移不超过 1 像素。
- 恢复“月下屋檐”的标志性圆月：月轮由 76×76 放大为 126×126，常驻可见度由 46% 提高到 78%，在浅色、深色和 Windows 默认壁纸上均清晰可见。
- 月轮改为完整柔边圆形并由人物自然遮挡，不再因越过宠物画布上沿形成生硬的直线裁切。

### 验证改进

- 图集构建新增坐姿头部锚点硬校验，任一动作在最终缩放排版后横向漂移超过 1.5 像素会直接构建失败。
- 新增月轮尺寸、有效像素与透明画布角测试，防止后续修改再次把月亮缩没或生成矩形背景。

## 2.4.2（2026-07-21）

### 新增

- 南宫婉“月下屋檐”改为可持续 20–60 秒的屋檐状态：18 帧自然落座、6 组随机驻留动作和 18 帧反向起身组成完整循环，右键菜单仍可随时手动触发。
- 站立版“栗糕轻尝”恢复菜单和自主触发；屋檐状态中的含栗、望月、闭目、夜风和回眸会在落座后随机出现。
- 图集构建新增桌面透明性硬检查：四个画布角必须透明，有效像素覆盖不得形成整块场景卡片。

### 改进与修复

- 删除运行画面中铺满宠物窗口的浅蓝色月面背景；月轮缩为人物右上方的局部装饰，屋檐只托住人物，不再显示方形底图、云层或飞鸟。
- 重画站立到落座的连续姿态，动作依次完成侧身、收衣、下落、扶檐和垂腿，入口时长由约 2.46 秒缩短到约 2.08 秒。
- 屋檐驻留动作减少静止权重、提高含栗与夜风等动作权重，最长停留由 90 秒缩短到 60 秒，减少长时间僵立。
- 2.4.1 的旧“月下含栗”图集和上一版屋檐图集继续作为独立 WebP 保留，便于比较和回退。

## 2.4.1（2026-07-21）

### 新增

- 南宫婉新增 36 帧、正常速度约 9.1 秒的招牌动作“月下含栗”：从完整月轮与屋檐远景推近到飞檐边缘坐姿，再切入紫色御物灵光、栗糕与嘴部特写，最后回到巨大局部月面下的坐姿。
- “月下含栗”可以从右键动作菜单随时播放，也会加入“动作展示”。开启“自主小动作”后，它以约 `1/20` 的基础权重参与随机触发，并有 90 秒自动冷却。

### 改进

- 旧“栗糕轻尝”的 10 帧素材与动作 ID 完整保留，但退出菜单、普通自主动作和动作展示，等待以后作为彩蛋使用。
- “动作展示”现在跳过既不显示菜单、也没有自主权重的休眠/彩蛋动作；隐藏但仍可自主触发的动作继续正常展示。
- 南宫婉图集扩展为 3072×5408，新增动作可跨三行连续取 36 帧，不改变 v3 宠物包格式。

## 2.4.0（2026-07-20）

### 新增

- 南宫婉的看向鼠标素材从 16 个关键方向扩展为 64 帧，每 5.625° 对应一档，转向时会沿圆周最短方向平滑经过中间姿态。
- “注视方式”新增“鼠标活动时（推荐）”和“始终看向鼠标”。推荐模式下，鼠标静止约 8 秒后会把控制交还给自动闲逛或自主小动作；鼠标再次移动时立即恢复注视。
- 右键菜单新增可记忆的“显示详情”。开启后每个菜单项右侧显示圆形问号，鼠标进入问号区域便立即显示说明，无需等待。
- 菜单说明会列出实际参数，包括等待时间、移动速度、动画倍率，以及当前宠物每个动作的帧数、时长、自动权重、概率、冷却和特效组。

### 改进与修复

- 看向鼠标现在优先于自动闲逛，解决同时勾选两项后宠物可能不再注视鼠标的问题。
- 大角度转向增加约 110 毫秒的平滑过程和每秒约 360° 的转向上限，减少跳方向。
- 详情说明会随当前宠物和 `pet.json` 动作定义动态更新，不再把动物动作名称套用到人物宠物上。

## 2.3.0（2026-07-19）

### 新增

- 新增第三个内置宠物“南宫婉”，包含 19 个动作和 7 组术法特效。
- 正式支持 v3 动态宠物包：宠物可自行定义动作数量、图集行列、每个动作的帧数和逐帧时长。
- 新增低频遁光移动，支持位移起止帧、屏幕距离比例、垂直范围、动作权重和冷却时间。
- 新增安静、标准、活跃三档闲逛强度，对应不同等待时间和移动范围。
- 新增动作行为组和同组冷却，避免相近特效连续触发。

### 改进与修复

- 动作多或动作少的宠物包都可以运行，不再要求固定数量的图集行。
- 修复没有视线动作的宠物在自主动作模式下可能无法正确触发空闲动作的问题。
- 继续兼容十一、紫灵及已有的 v2 固定图集宠物包。

## 2.2.0（2026-07-18）

### 新增

- 单击宠物会随机回应，双击播放跳跃，拖动时不会误触单击动作。
- 新增自主小动作：宠物空闲时可以随机播放不产生位移的动作。
- 新增“动作展示”，可以连续查看当前宠物的原地动作。
- `pet.json` 可以为每只宠物定义自己的动作名称、菜单顺序和自动播放权重。

### 改进

- 菜单会随宠物切换动作名称，使人物和动物都能使用贴合自身的文字。
- 保持对旧版宠物包的兼容。

## 2.1.0（2026-07-18）

### 新增

- 支持统一的 v2 宠物包格式。把包含 `pet.json` 和 `spritesheet.webp` 的文件夹放进用户宠物目录，重新扫描后即可使用。
- 新增动态宠物发现、格式校验、宠物目录入口和“重新扫描宠物”。
- 托盘图标会跟随当前宠物切换，并在提示中显示当前宠物名称。

### 改进

- 损坏或不符合规范的用户宠物包会被单独忽略，不会阻止内置宠物启动。
- 覆盖升级会保留用户安装的宠物包。

## 2.0.0（2026-07-18）

### 新增

- 项目由“十一桌面宠物”更名为“桌面灵伴”。
- 新增紫灵，与原有十一组成首套双宠物阵容。
- 右键菜单支持即时切换宠物，并会记住上次使用的角色。
- 新增统一的多宠物资源结构，为后续外部宠物包做准备。

### 改进

- 增加系统托盘图标、开始菜单入口和桌面快捷方式。
- 保留休息、移动、招手、跳跃、看向鼠标、自动闲逛和数字快捷键等第一版功能。

## 1.0.0（2026-07-18）

### 首次发布

- 发布最初的“十一桌面宠物”。
- 支持透明置顶窗口、拖动、16 方向看向鼠标和自动闲逛。
- 提供休息、左右移动、招手、跳跃等基础动作及数字快捷键。
- 支持托盘菜单、当前位置与大小速度设置记忆、当前用户开机启动。
- 提供无需安装 Python 或 Qt 的 Windows 安装程序。
